 import 'package:flutter/material.dart';

TextStyle textStying({required Color color, required FontWeight weight, required double size}) {
    return TextStyle(
                          color: color, 
                          fontWeight: weight,
                          fontSize: size,
                          );
  }